@model System.DateTime?

@if (Model.HasValue)
{
    @Html.TextBox("",Model.Value.ToString("yyyy/MM/dd"), new { @class = "text datebox", style = "width:120px" })
}
else
{
    @Html.TextBox("", DateTime.Now.ToString("yyyy/MM/dd"), new { @class = "text datebox", style = "width:120px" })
}